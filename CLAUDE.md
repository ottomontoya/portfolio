# Agent delegation policy

Any agent running on Sonnet tier or above (Sonnet, Opus, Fable) may create subagents for task delegation, regardless of its configured reasoning effort. This permission does not extend to Haiku-tier agents.

# Browser preview policy

Do not use Playwright for browser previews. Use the t3-code preview tools (`mcp__t3-code__preview_*`) exclusively.
